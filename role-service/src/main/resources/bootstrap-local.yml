spring:
  cloud:
    config:
      enabled: false
      discovery:
        enabled: false
    discovery:
      enabled: false
  liquibase:
    change-log: classpath:db/changelog/changelog-role-service-master.xml
    drop-first: false
    enabled: true
  jpa:
    hibernate:
      ddl-auto: none

