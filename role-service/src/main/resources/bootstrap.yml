spring:
  application:
    name: role-service
  cloud:
    config:
      enabled: false
      discovery:
        enabled: false
    discovery:
      enabled: true

eureka:
  client:
    serviceUrl:
      defaultZone: http://placeholder:8761/eureka/
