{
  "name": "mts-services",
  "version": "1.0.0",
  "description": "A base repo for all microservices",
  "main": "index.js",
  "scripts": {
    "test:awesome": "mocha specs/*/*.js --reporter mochawesome || true",
    "open:report": "open mochawesome-report/mochawesome.html",
    "test": "mocha specs/*/*.js",
    "test-ci": "mocha specs/*/*.js --reporter mocha-junit-reporter"
  },
  "devDependencies": {
    "chai": "^3.5.0",
    "mocha": "^8.2.1",
    "mocha-junit-reporter": "^2.0.0",
    "mochawesome": "^3.1.2",
    "supertest": "^1.2.0"
  },
  "dependencies": {
    "dotenv": "^8.2.0",
    "http-status-codes": "^2.1.4",
    "qs": ">=6.0.4"
  }
}
