spring:
  cloud:
    config:
      enabled: false
      discovery:
        enabled: false
    discovery:
      enabled: true
# Ribbon is a client-side load balancer, this property disables the use of Eureka in Ribbon
ribbon:
  eureka:
    enabled: false
