spring:
  cloud:
    config:
      discovery:
        enabled: false

eureka:
  client:
    fetch-registry: true
    registerWithEureka: true
    serviceUrl:
      defaultZone: http://placeholder:8761/eureka/
