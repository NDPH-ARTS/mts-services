spring:
  application:
    name: config-server
  cloud:
    config:
      server:
        git:
          #TODO: placeholder
          uri: https://github.com/NDPH-ARTS/spring-config-server.git
          #TODO: placeholder
          search-paths: practitioner-service

server:
  port: 8888

eureka:
  client:
    serviceUrl:
      defaultZone: http://${EUREKA_HOSTNAME:localhost}:${EUREKA_PORT:8761}/eureka/
