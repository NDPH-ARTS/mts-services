spring:
  application:
    name: config-server
  cloud:
    config:
      server:
        git:
          uri: https://placeholder
          search-paths: placeholder
    inetutils:
      ignored-interfaces:
        - eth0
        - eth1
        - eth2
        - eth3
        - lo

server:
  port: 8888

eureka:
  client:
    serviceUrl:
      defaultZone: http://placeholder:8761/eureka/
