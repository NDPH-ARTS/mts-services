server:
  port: 8888
spring:
  cloud:
    config:
      server:
        git:
          default-label: main
management:
  endpoints:
    web:
      exposure:
        include: "*"
  health:
    probes:
      enabled: true
