{
  "name": "mts-services",
  "version": "1.0.0",
  "description": "A base repo for all microservices",
  "main": "index.js",
  "scripts": {
    "test:awesome": "mocha api-tests/specs/*.js --reporter mochawesome || true",
    "open:report": "open mochawesome-report/mochawesome.html",
    "test": "mocha api-tests/specs/*.js"
  },
  "devDependencies": {
    "chai": "^3.5.0",
    "mocha": "^2.5.3",
    "mochawesome": "^3.1.2",
    "supertest": "^1.2.0"
  },
  "dependencies": {
    "http-status-codes": "^2.1.4"
  }
}
