spring:
  application:
    name: init-service
  cloud:
    config:
      discovery:
        enabled: true
        service-id: CONFIG-SERVER

eureka:
  client:
    serviceUrl:
      defaultZone: http://placeholder:8761/eureka/
    registerWithEureka: false
